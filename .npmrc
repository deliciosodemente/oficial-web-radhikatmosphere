# NVIDIA Omniverse registry configuration
@nvidia:registry=https://nvcr.io/ns/nvidia/omniverse
//nvcr.io/ns/nvidia/omniverse/:_authToken=%NVIDIA_NGC_API_KEY%

# Configurar variable temporal en PowerShell:
# $env:NVIDIA_NGC_API_KEY='TU_TOKEN_AQUI'
always-auth=true
# Make sure to set NVIDIA_NGC_API_KEY environment variable
# with your NGC authentication token before running npm commands